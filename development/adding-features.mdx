---
title: 'Adding Features'
description: 'Step-by-step guide to extending Sistema Financiero'
icon: 'code'
---

## Architecture Overview

Sistema Financiero follows a **full-stack Next.js architecture** with clear separation of concerns:

```
┌─────────────────────────────────────────────────────┐
│         Next.js 15 (Full-Stack Framework)           │
├─────────────────────────────────────────────────────┤
│  FRONTEND (Client-Side)                             │
│  ├─ app/page.tsx          → Dashboard               │
│  ├─ app/registro/         → Manual forms            │
│  ├─ app/agente-mejorado/  → AI chat                 │
│  └─ components/*.tsx      → Reusable UI             │
├─────────────────────────────────────────────────────┤
│  BACKEND (Server-Side)                              │
│  └─ app/api/              → API Routes              │
│      ├─ transacciones/route.ts  → GET transactions │
│      ├─ chat/stream/route.ts    → AI streaming     │
│      └─ upload-image/route.ts   → OCR processing   │
├─────────────────────────────────────────────────────┤
│  DATABASE (Supabase)                                │
│  └─ transacciones table                             │
└─────────────────────────────────────────────────────┘
```

## Common Patterns

### Pattern 1: Adding a New API Endpoint

<Steps>
  <Step title="Create Route File">
    Create a new route handler in `app/api/`:

    ```typescript
    // File: app/api/stats/route.ts
    import { NextRequest, NextResponse } from 'next/server'
    import { createClient } from '@supabase/supabase-js'

    const supabase = createClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
    )

    export async function GET(request: NextRequest) {
      const { data, error } = await supabase
        .from('transacciones')
        .select('tipo, monto')

      if (error) {
        return NextResponse.json({ error: error.message }, { status: 500 })
      }

      const stats = data.reduce(
        (acc, t) => {
          if (t.tipo === 'ingreso') {
            acc.totalIngresos += Number(t.monto)
          } else {
            acc.totalGastos += Number(t.monto)
          }
          return acc
        },
        { totalIngresos: 0, totalGastos: 0 }
      )

      return NextResponse.json({ data: stats })
    }
    ```
  </Step>

  <Step title="Call from Frontend">
    Use the endpoint in your component:

    ```typescript
    const [stats, setStats] = useState({ totalIngresos: 0, totalGastos: 0 })

    useEffect(() => {
      fetch('/api/stats')
        .then(res => res.json())
        .then(json => setStats(json.data))
    }, [])
    ```
  </Step>
</Steps>

<Note>
  API routes in `app/api/` are automatically server-side and have access to environment variables.
</Note>

### Pattern 2: Adding a New Category

<Warning>
  Categories are **hardcoded** in multiple files. Update all locations to avoid inconsistencies.
</Warning>

<Steps>
  <Step title="Update AI Chat Categories">
    File: `app/api/chat/stream/route.ts:26`

    ```typescript
    📋 CATEGORÍAS VÁLIDAS:
    **Gastos:** Alimentación, Transporte, Vivienda, Salud, Entretenimiento, Educación, Mascotas, Otros Gastos
    **Ingresos:** Salario, Ventas, Servicios, Inversiones, Otros Ingresos
    ```
  </Step>

  <Step title="Update Function Definitions">
    File: `app/api/chat/stream/route.ts:75`

    ```typescript
    categoria: {
      type: 'string',
      enum: [
        'Alimentación',
        'Transporte',
        'Vivienda',
        'Salud',
        'Entretenimiento',
        'Educación',
        'Mascotas', // ← New category
        'Otros Gastos'
      ]
    }
    ```
  </Step>

  <Step title="Update Manual Form">
    File: `app/registro/page.tsx` (add to category dropdown)

    ```typescript
    const CATEGORIAS_GASTOS = [
      'Alimentación',
      'Transporte',
      'Vivienda',
      'Salud',
      'Entretenimiento',
      'Educación',
      'Mascotas', // ← New category
      'Otros Gastos'
    ]
    ```
  </Step>

  <Step title="Update OCR Analysis">
    File: `app/api/upload-image/route.ts:64`

    ```typescript
    **CATEGORÍAS VÁLIDAS DEL SISTEMA:**
    - Gastos: Alimentación, Transporte, Vivienda, Salud, Entretenimiento, Educación, Mascotas, Otros Gastos
    ```
  </Step>
</Steps>

### Pattern 3: Adding a Database Column

<Steps>
  <Step title="Add Column to Supabase">
    Run in Supabase SQL Editor:

    ```sql
    ALTER TABLE transacciones ADD COLUMN notas TEXT;
    ```
  </Step>

  <Step title="Update TypeScript Interface">
    File: `components/DataViews.tsx:8`

    ```typescript
    interface Transaccion {
      id: string
      fecha: string
      tipo: 'gasto' | 'ingreso'
      categoria: string
      monto: number
      descripcion: string
      metodo_pago: string
      notas: string | null // ← New field
    }
    ```
  </Step>

  <Step title="Update API Insert Logic">
    File: `app/api/chat/stream/route.ts:208`

    ```typescript
    const { error } = await supabase.from('transacciones').insert({
      tipo,
      monto: functionArgs.monto,
      categoria: functionArgs.categoria,
      concepto: functionArgs.descripcion || `${tipo} - ${functionArgs.categoria}`,
      descripcion: functionArgs.descripcion || null,
      metodo_pago: functionArgs.metodo_pago || 'Efectivo',
      registrado_por: functionArgs.registrado_por || 'Usuario',
      notas: functionArgs.notas || null, // ← New field
      fecha: new Date().toISOString(),
    })
    ```
  </Step>

  <Step title="Update AI Function Schema">
    Add to function parameters:

    ```typescript
    parameters: {
      type: 'object',
      properties: {
        monto: { type: 'number' },
        categoria: { type: 'string', enum: [...] },
        descripcion: { type: 'string' },
        metodo_pago: { type: 'string', enum: [...] },
        registrado_por: { type: 'string' },
        notas: { type: 'string', description: 'Notas adicionales' } // ← New
      },
      required: ['monto', 'categoria']
    }
    ```
  </Step>
</Steps>

### Pattern 4: Adding a New Component

<Steps>
  <Step title="Create Component File">
    ```typescript
    // File: components/ExpenseChart.tsx
    'use client'

    import { useState, useEffect } from 'react'
    import { Pie } from 'react-chartjs-2'

    export function ExpenseChart() {
      const [chartData, setChartData] = useState(null)

      useEffect(() => {
        fetch('/api/transacciones?vista=mensual')
          .then(res => res.json())
          .then(json => {
            const gastos = json.data.filter(t => t.tipo === 'gasto')
            const byCategory = gastos.reduce((acc, t) => {
              acc[t.categoria] = (acc[t.categoria] || 0) + Number(t.monto)
              return acc
            }, {})

            setChartData({
              labels: Object.keys(byCategory),
              datasets: [{
                data: Object.values(byCategory),
                backgroundColor: ['#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0']
              }]
            })
          })
      }, [])

      if (!chartData) return <div>Loading...</div>

      return <Pie data={chartData} />
    }
    ```
  </Step>

  <Step title="Import and Use">
    ```typescript
    // File: app/page.tsx
    import { ExpenseChart } from '@/components/ExpenseChart'

    export default function Dashboard() {
      return (
        <div>
          <h1>Dashboard</h1>
          <ExpenseChart />
        </div>
      )
    }
    ```
  </Step>
</Steps>

## Feature Examples

### Example 1: Add Budget Tracking

<CodeGroup>
```sql SQL Migration
-- Add budget column to transacciones
ALTER TABLE transacciones ADD COLUMN presupuesto NUMERIC(10, 2);

-- Create budget alerts table
CREATE TABLE presupuestos (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  usuario_id UUID REFERENCES auth.users(id),
  categoria TEXT NOT NULL,
  monto_limite NUMERIC(10, 2) NOT NULL,
  periodo TEXT CHECK (periodo IN ('mensual', 'semanal', 'diario')),
  created_at TIMESTAMP DEFAULT NOW()
);
```

```typescript API Endpoint
// File: app/api/presupuesto/route.ts
import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)

export async function GET() {
  const { data: presupuestos } = await supabase
    .from('presupuestos')
    .select('*')

  const { data: gastos } = await supabase
    .from('transacciones')
    .select('categoria, monto')
    .eq('tipo', 'gasto')
    .gte('fecha', new Date(new Date().setDate(1)).toISOString())

  const gastoPorCategoria = gastos.reduce((acc, g) => {
    acc[g.categoria] = (acc[g.categoria] || 0) + Number(g.monto)
    return acc
  }, {})

  const alertas = presupuestos.map(p => ({
    categoria: p.categoria,
    limite: p.monto_limite,
    gastado: gastoPorCategoria[p.categoria] || 0,
    porcentaje: ((gastoPorCategoria[p.categoria] || 0) / p.monto_limite) * 100
  }))

  return NextResponse.json({ data: alertas })
}
```

```typescript Component
// File: components/BudgetAlerts.tsx
'use client'
import { useState, useEffect } from 'react'

export function BudgetAlerts() {
  const [alertas, setAlertas] = useState([])

  useEffect(() => {
    fetch('/api/presupuesto')
      .then(res => res.json())
      .then(json => setAlertas(json.data))
  }, [])

  return (
    <div className="space-y-4">
      <h3 className="text-lg font-bold">Alertas de Presupuesto</h3>
      {alertas.map(a => (
        <div key={a.categoria} className="p-4 rounded-lg bg-white shadow">
          <h4>{a.categoria}</h4>
          <div className="w-full bg-gray-200 rounded-full h-4">
            <div
              className={`h-4 rounded-full ${
                a.porcentaje > 90 ? 'bg-red-500' : 'bg-green-500'
              }`}
              style={{ width: `${Math.min(a.porcentaje, 100)}%` }}
            />
          </div>
          <p className="text-sm text-gray-600">
            ${a.gastado.toFixed(2)} / ${a.limite.toFixed(2)} ({a.porcentaje.toFixed(0)}%)
          </p>
        </div>
      ))}
    </div>
  )
}
```
</CodeGroup>

### Example 2: Add Recurring Transactions

<CodeGroup>
```sql SQL Schema
CREATE TABLE transacciones_recurrentes (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  usuario_id UUID REFERENCES auth.users(id),
  tipo TEXT CHECK (tipo IN ('ingreso', 'gasto')) NOT NULL,
  monto NUMERIC(10, 2) NOT NULL,
  categoria TEXT NOT NULL,
  descripcion TEXT,
  frecuencia TEXT CHECK (frecuencia IN ('diaria', 'semanal', 'mensual', 'anual')),
  dia_mes INTEGER CHECK (dia_mes BETWEEN 1 AND 31),
  activo BOOLEAN DEFAULT true,
  proxima_fecha DATE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_recurrentes_proxima_fecha ON transacciones_recurrentes(proxima_fecha);
```

```typescript API Route
// File: app/api/gastos-recurrentes/procesar/route.ts
import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)

export async function POST() {
  const hoy = new Date().toISOString().split('T')[0]

  // 1. Obtener transacciones recurrentes que vencen hoy
  const { data: recurrentes } = await supabase
    .from('transacciones_recurrentes')
    .select('*')
    .eq('activo', true)
    .lte('proxima_fecha', hoy)

  if (!recurrentes || recurrentes.length === 0) {
    return NextResponse.json({ message: 'No hay recurrentes para procesar' })
  }

  // 2. Crear transacciones reales
  const transacciones = recurrentes.map(r => ({
    tipo: r.tipo,
    monto: r.monto,
    categoria: r.categoria,
    concepto: `Recurrente: ${r.descripcion}`,
    descripcion: r.descripcion,
    metodo_pago: 'Automático',
    registrado_por: 'Sistema',
    fecha: new Date().toISOString(),
  }))

  await supabase.from('transacciones').insert(transacciones)

  // 3. Actualizar próxima fecha
  for (const r of recurrentes) {
    let proximaFecha = new Date(r.proxima_fecha)

    if (r.frecuencia === 'mensual') {
      proximaFecha.setMonth(proximaFecha.getMonth() + 1)
    } else if (r.frecuencia === 'semanal') {
      proximaFecha.setDate(proximaFecha.getDate() + 7)
    } else if (r.frecuencia === 'diaria') {
      proximaFecha.setDate(proximaFecha.getDate() + 1)
    }

    await supabase
      .from('transacciones_recurrentes')
      .update({ proxima_fecha: proximaFecha.toISOString().split('T')[0] })
      .eq('id', r.id)
  }

  return NextResponse.json({ procesadas: recurrentes.length })
}
```
</CodeGroup>

## Changing AI Model

File: `app/api/chat/stream/route.ts:54`

```typescript
const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
  body: JSON.stringify({
    model: 'anthropic/claude-3.5-sonnet', // ← Change model here
    // Available models:
    // - 'google/gemini-2.5-flash' (default, fastest)
    // - 'anthropic/claude-3.5-sonnet' (best quality)
    // - 'openai/gpt-4o' (balanced)
    // - 'meta-llama/llama-3.1-70b-instruct' (open source)
  })
})
```

<Note>
  Check [OpenRouter Models](https://openrouter.ai/models) for full list and pricing.
</Note>

## Custom Hooks

### Example: useTransactions Hook

```typescript
// File: hooks/useTransactions.ts
import { useState, useEffect } from 'react'
import { supabase } from '@/lib/supabase'

interface Transaccion {
  id: string
  fecha: string
  tipo: 'gasto' | 'ingreso'
  categoria: string
  monto: number
}

export function useTransactions(vista: 'diaria' | 'semanal' | 'mensual' = 'mensual') {
  const [transacciones, setTransacciones] = useState<Transaccion[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    async function fetchData() {
      try {
        const { data, error } = await supabase
          .from('transacciones')
          .select('*')
          .order('fecha', { ascending: false })

        if (error) throw error

        setTransacciones(data || [])
      } catch (err: any) {
        setError(err.message)
      } finally {
        setLoading(false)
      }
    }

    fetchData()
  }, [vista])

  return { transacciones, loading, error }
}
```

## Testing New Features

<Steps>
  <Step title="Local Testing">
    ```bash
    npm run dev
    ```
    Test in browser at http://localhost:3000
  </Step>

  <Step title="Check Console Logs">
    Open DevTools → Console to see errors or API responses
  </Step>

  <Step title="Test Database Changes">
    Use Supabase Dashboard → Table Editor to verify inserts
  </Step>

  <Step title="Test API Endpoints">
    Use `curl` or Postman:
    ```bash
    curl http://localhost:3000/api/stats
    ```
  </Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Database Schema" icon="database" href="/development/database-schema">
    Learn about the database structure
  </Card>
  <Card title="Customization Guide" icon="palette" href="/development/customization">
    Customize categories, styles, and behavior
  </Card>
</CardGroup>